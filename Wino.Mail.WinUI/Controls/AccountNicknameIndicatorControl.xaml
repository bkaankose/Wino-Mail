<UserControl
    x:Class="Wino.Controls.AccountNicknameIndicatorControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="using:Wino.Helpers"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="root"
    x:DefaultBindMode="OneWay"
    mc:Ignorable="d">

    <Border
        MinHeight="16"
        Padding="6,1"
        VerticalAlignment="Center"
        Background="{ThemeResource MailListHeaderBackgroundColor}"
        BorderBrush="{x:Bind helpers:XamlHelpers.GetSolidColorBrushFromHex(AccountColorHex)}"
        BorderThickness="1"
        CornerRadius="4">
        <TextBlock
            FontSize="10"
            FontWeight="SemiBold"
            Foreground="{ThemeResource ApplicationForegroundThemeBrush}"
            Text="{x:Bind AccountNickname}"
            TextTrimming="CharacterEllipsis" />
    </Border>
</UserControl>
