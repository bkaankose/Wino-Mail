<abstract:IdlePageAbstract
    x:Class="Wino.Views.Mail.IdlePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:abstract="using:Wino.Views.Abstract"
    xmlns:controls="using:Wino.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="using:Wino.Helpers"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <StackPanel
            MaxWidth="440"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Spacing="12"
            Visibility="{x:Bind ViewModel.IsMailEmptyStateVisible, Mode=OneWay}">
            <TextBlock
                HorizontalAlignment="Center"
                Style="{StaticResource TitleTextBlockStyle}"
                Text="{x:Bind ViewModel.MailEmptyStateTitle, Mode=OneWay}"
                TextAlignment="Center"
                TextWrapping="WrapWholeWords" />
            <TextBlock
                HorizontalAlignment="Center"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Style="{StaticResource BodyTextBlockStyle}"
                Text="{x:Bind ViewModel.MailEmptyStateMessage, Mode=OneWay}"
                TextAlignment="Center"
                TextWrapping="WrapWholeWords" />
            <StackPanel
                HorizontalAlignment="Center"
                Orientation="Horizontal"
                Spacing="12">
                <Button
                    AutomationProperties.AutomationId="IdlePageButton"
                    Command="{x:Bind ViewModel.AddAccountCommand}"
                    Content="{x:Bind ViewModel.AddAccountText, Mode=OneWay}"
                    Style="{ThemeResource AccentButtonStyle}" />
                <Button
                    AutomationProperties.AutomationId="IdlePageButton2"
                    Command="{x:Bind ViewModel.ManageAccountsCommand}"
                    Content="{x:Bind ViewModel.ManageAccountsText, Mode=OneWay}" />
            </StackPanel>
        </StackPanel>

        <StackPanel
            MaxWidth="440"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Spacing="12"
            Visibility="{x:Bind ViewModel.IsCompanionUnavailableStateVisible, Mode=OneWay}">
            <TextBlock
                HorizontalAlignment="Center"
                Style="{StaticResource TitleTextBlockStyle}"
                Text="{x:Bind ViewModel.CompanionUnavailableTitle, Mode=OneWay}"
                TextAlignment="Center"
                TextWrapping="WrapWholeWords" />
            <TextBlock
                HorizontalAlignment="Center"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Style="{StaticResource BodyTextBlockStyle}"
                Text="{x:Bind ViewModel.CompanionUnavailableMessage, Mode=OneWay}"
                TextAlignment="Center"
                TextWrapping="WrapWholeWords" />
            <Button
                HorizontalAlignment="Center"
                AutomationProperties.AutomationId="CompanionUnavailableRetryButton"
                Command="{x:Bind ViewModel.RetryCompanionConnectionCommand}"
                Content="{x:Bind ViewModel.RetryCompanionConnectionText, Mode=OneWay}"
                Style="{ThemeResource AccentButtonStyle}" />
        </StackPanel>
    </Grid>

</abstract:IdlePageAbstract>
