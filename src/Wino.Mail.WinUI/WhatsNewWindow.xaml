<?xml version="1.0" encoding="utf-8" ?>
<winuiex:WindowEx
    x:Class="Wino.Mail.WinUI.WhatsNewWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:winuiex="using:WinUIEx"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource WinoApplicationBackgroundColor}">
        <Frame x:Name="RootFrame" />

        <!--
            The content extends into the title bar. This strip is the drag region; it sits above the
            page header, which holds no interactive element, and leaves the close button to the system.
        -->
        <Border
            x:Name="TitleBarDragRegion"
            Height="48"
            VerticalAlignment="Top"
            Background="Transparent" />
    </Grid>
</winuiex:WindowEx>
