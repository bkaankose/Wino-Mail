<ContentDialog
    x:Class="Wino.Mail.Dialogs.OAuthSignInDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:domain="using:Wino.Core.Domain"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="{x:Bind domain:Translator.OAuthSignInDialog_Title, Mode=OneTime}"
    CloseButtonText="{x:Bind domain:Translator.Buttons_Cancel, Mode=OneTime}"
    Opened="OnDialogOpened"
    Style="{StaticResource WinoDialogStyle}"
    mc:Ignorable="d">

    <Grid Width="460" Height="580">
        <WebView2 x:Name="AuthWebView" />
        <ProgressRing
            x:Name="LoadingRing"
            Width="40"
            Height="40"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            IsActive="True" />
    </Grid>
</ContentDialog>
