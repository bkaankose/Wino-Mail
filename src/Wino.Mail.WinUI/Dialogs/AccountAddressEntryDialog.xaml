<ContentDialog
    x:Class="Wino.Dialogs.AccountAddressEntryDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:domain="using:Wino.Core.Domain"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="{x:Bind domain:Translator.AccountAddressEntryDialog_Title}"
    CloseButtonText="{x:Bind domain:Translator.Buttons_Cancel}"
    DefaultButton="Primary"
    IsPrimaryButtonEnabled="False"
    PrimaryButtonClick="UseAddressClicked"
    PrimaryButtonText="{x:Bind domain:Translator.AccountAddressEntryDialog_UseAddressButton}"
    Style="{StaticResource WinoDialogStyle}"
    mc:Ignorable="d">

    <ContentDialog.Resources>
        <x:Double x:Key="ContentDialogMinWidth">360</x:Double>
    </ContentDialog.Resources>

    <StackPanel Spacing="8">
        <TextBlock
            Foreground="{ThemeResource SystemFillColorCriticalBrush}"
            Style="{ThemeResource BodyTextBlockStyle}"
            Text="{x:Bind domain:Translator.AccountAddressEntryDialog_ConflictWarning}"
            TextWrapping="Wrap" />
        <TextBlock
            FontWeight="SemiBold"
            Style="{ThemeResource BodyTextBlockStyle}"
            Text="{x:Bind ConflictingAddress}" />
        <TextBlock
            Style="{ThemeResource BodyTextBlockStyle}"
            Text="{x:Bind domain:Translator.AccountAddressEntryDialog_Description}"
            TextWrapping="Wrap" />
        <TextBox
            x:Name="AddressBox"
            AutomationProperties.AutomationId="AccountAddressEntryDialogAddressBox"
            Header="{x:Bind domain:Translator.AccountAddressEntryDialog_AddressHeader}"
            InputScope="EmailSmtpAddress"
            IsSpellCheckEnabled="False"
            TextChanged="AddressChanged" />
    </StackPanel>
</ContentDialog>
