<ContentDialog
    x:Class="Wino.Mail.WinUI.Dialogs.NewAccountDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:accounts="using:Wino.Core.Domain.Models.Accounts"
    xmlns:coreControls="using:Wino.Mail.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:domain="using:Wino.Core.Domain"
    xmlns:helpers="using:Wino.Helpers"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    Title="{x:Bind domain:Translator.NewAccountDialog_Title}"
    HorizontalContentAlignment="Stretch"
    DefaultButton="Primary"
    IsPrimaryButtonEnabled="False"
    Opened="DialogOpened"
    PrimaryButtonClick="CreateClicked"
    PrimaryButtonText="{x:Bind domain:Translator.Buttons_CreateAccount}"
    SecondaryButtonClick="CancelClicked"
    SecondaryButtonText="{x:Bind domain:Translator.Buttons_Cancel}"
    Style="{StaticResource WinoDialogStyle}"
    mc:Ignorable="d">

    <ContentDialog.Resources>
        <DataTemplate x:Key="NewMailProviderTemplate" x:DataType="accounts:ProviderDetail">
            <Grid
                Margin="0,4"
                Padding="12,10"
                AutomationProperties.HelpText="{x:Bind Description}"
                AutomationProperties.Name="{x:Bind Name}"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Image
                    Width="32"
                    Height="32"
                    Source="{x:Bind ProviderImage}" />
                <StackPanel
                    Grid.Column="1"
                    Margin="12,0"
                    VerticalAlignment="Center"
                    Spacing="2">
                    <TextBlock Style="{ThemeResource BodyStrongTextBlockStyle}" Text="{x:Bind Name}" />
                    <TextBlock
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Style="{ThemeResource CaptionTextBlockStyle}"
                        Text="{x:Bind Description}" />
                </StackPanel>
            </Grid>
        </DataTemplate>
    </ContentDialog.Resources>

    <Grid MinWidth="400" RowSpacing="12">
        <Grid Visibility="{x:Bind IsProviderSelectionVisible, Mode=OneWay}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!--  Account Name  -->

            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBox
                    x:Name="AccountNameTextbox"
                    AutomationProperties.AutomationId="AccountNameTextbox"
                    Header="{x:Bind domain:Translator.NewAccountDialog_AccountName}"
                    PlaceholderText="{x:Bind domain:Translator.NewAccountDialog_AccountNamePlaceholder}"
                    TextChanged="InputChanged" />

                <Button
                    Grid.Column="1"
                    VerticalAlignment="Bottom"
                    AutomationProperties.AutomationId="XBindDomainTranslatorAccountDetailsPageColorPickerTitle"
                    AutomationProperties.HelpText="{x:Bind domain:Translator.AccountDetailsPage_ColorPicker_Description}"
                    AutomationProperties.Name="{x:Bind domain:Translator.AccountDetailsPage_ColorPicker_Title}">
                    <Grid>
                        <Ellipse
                            x:Name="SelectedColorEllipse"
                            Width="16"
                            Height="16" />

                        <TextBlock x:Name="PickColorTextblock" Text="{x:Bind domain:Translator.CalendarDisplayOptions_Color}" />
                    </Grid>

                    <Button.Flyout>
                        <Flyout Placement="TopEdgeAlignedLeft">
                            <GridView
                                Width="150"
                                AutomationProperties.AutomationId="XBindDomainTranslatorAccountDetailsPageColorPickerTitle2"
                                AutomationProperties.Name="{x:Bind domain:Translator.AccountDetailsPage_ColorPicker_Title}"
                                ItemTemplate="{StaticResource AccountColorTemplate}"
                                ItemsSource="{x:Bind AvailableColors, Mode=OneWay}"
                                SelectedItem="{x:Bind SelectedColor, Mode=TwoWay}" />
                        </Flyout>
                    </Button.Flyout>
                </Button>

            </Grid>

            <Border
                x:Name="InitialSynchronizationPanel"
                Grid.Row="1"
                Margin="0,12,0,0"
                Padding="12"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Visibility="Collapsed">
                <StackPanel Spacing="10">
                    <StackPanel Spacing="2">
                        <TextBlock FontWeight="SemiBold" Text="{x:Bind domain:Translator.AccountCreation_InitialSynchronization_Title}" />
                        <TextBlock
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            Style="{StaticResource CaptionTextBlockStyle}"
                            Text="{x:Bind domain:Translator.AccountCreation_InitialSynchronization_Description}"
                            TextWrapping="WrapWholeWords" />
                    </StackPanel>

                    <ComboBox
                        x:Name="InitialSynchronizationComboBox"
                        HorizontalAlignment="Stretch"
                        AutomationProperties.AutomationId="InitialSynchronizationComboBox"
                        AutomationProperties.HelpText="{x:Bind domain:Translator.AccountCreation_InitialSynchronization_Description}"
                        AutomationProperties.Name="{x:Bind domain:Translator.AccountCreation_InitialSynchronization_Title}"
                        Header="{x:Bind domain:Translator.AccountCreation_InitialSynchronization_Title}"
                        ItemsSource="{x:Bind InitialSynchronizationRanges, Mode=OneWay}"
                        SelectionChanged="InitialSynchronizationSelectionChanged">
                        <ComboBox.ItemTemplate>
                            <DataTemplate x:DataType="accounts:InitialSynchronizationRangeOption">
                                <TextBlock Text="{x:Bind DisplayText}" />
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>

                    <muxc:InfoBar
                        x:Name="InitialSynchronizationWarningBar"
                        Title="{x:Bind domain:Translator.GeneralTitle_Warning}"
                        IsOpen="True"
                        Message="{x:Bind domain:Translator.AccountCreation_InitialSynchronization_EverythingWarning}"
                        Severity="Warning"
                        Visibility="Collapsed" />
                </StackPanel>
            </Border>


            <ListView
                Grid.Row="2"
                Margin="0,12"
                Padding="0"
                AutomationProperties.AutomationId="NewAccountDialogListView"
                ItemTemplate="{StaticResource NewMailProviderTemplate}"
                ItemsSource="{x:Bind Providers}"
                SelectedItem="{x:Bind SelectedMailProvider, Mode=TwoWay}"
                SelectionMode="Single">
                <ListView.ItemContainerStyle>
                    <Style BasedOn="{StaticResource DefaultListViewItemStyle}" TargetType="ListViewItem">
                        <Setter Property="Margin" Value="0,2" />
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="CornerRadius" Value="6" />
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>
        </Grid>

        <!--  Known special IMAP login details.  -->
        <Grid RowSpacing="16" Visibility="{x:Bind IsSpecialImapServerPartVisible, Mode=OneWay}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Button AutomationProperties.AutomationId="NewAccountDialogButton" Click="BackClicked">
                <Button.Content>
                    <Viewbox Width="16">
                        <coreControls:WinoFontIcon Icon="ArrowLeft" />
                    </Viewbox>
                </Button.Content>
            </Button>

            <Image
                Width="150"
                Height="50"
                HorizontalAlignment="Center"
                Source="{x:Bind SelectedMailProvider.ProviderImage, Mode=OneWay}" />

            <TextBox
                x:Name="DisplayNameTextBox"
                Grid.Row="1"
                AutomationProperties.AutomationId="DisplayNameTextBox"
                Header="Display Name"
                PlaceholderText="eg. John Doe"
                TextChanged="InputChanged" />

            <TextBox
                x:Name="SpecialImapAddress"
                Grid.Row="2"
                AutomationProperties.AutomationId="SpecialImapAddress"
                Header="E-mail Address"
                PlaceholderText="eg. johndoe@testmail.com"
                TextChanged="InputChanged" />

            <PasswordBox
                x:Name="AppSpecificPassword"
                Grid.Row="3"
                AutomationProperties.AutomationId="AppSpecificPassword"
                Header="App-Specific Password"
                PasswordChanged="ImapPasswordChanged" />

            <ComboBox
                Grid.Row="4"
                AutomationProperties.AutomationId="NewAccountDialogComboBox"
                Header="{x:Bind domain:Translator.ImapCalDavSettingsPage_CalendarModeHeader}"
                ItemsSource="{x:Bind CalendarModeOptions, Mode=OneWay}"
                SelectedIndex="{x:Bind SelectedCalendarModeIndex, Mode=TwoWay}" />

            <HyperlinkButton
                Grid.Row="5"
                HorizontalAlignment="Right"
                AutomationProperties.AutomationId="NewAccountDialogHyperlinkButton"
                Click="AppSpecificHelpButtonClicked"
                Content="How do I get app-specific password?" />
        </Grid>
    </Grid>
</ContentDialog>
