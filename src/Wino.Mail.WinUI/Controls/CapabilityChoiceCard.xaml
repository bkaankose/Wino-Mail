<UserControl
    x:Class="Wino.Controls.CapabilityChoiceCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:domain="using:Wino.Core.Domain"
    xmlns:helpers="using:Wino.Helpers"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="root"
    HorizontalAlignment="Stretch"
    VerticalAlignment="Stretch"
    x:DefaultBindMode="OneWay"
    mc:Ignorable="d">

    <Button
        Padding="14"
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch"
        HorizontalContentAlignment="Stretch"
        VerticalContentAlignment="Top"
        AutomationProperties.AutomationId="CapabilityChoiceCardButton"
        AutomationProperties.HelpText="{x:Bind Description}"
        AutomationProperties.Name="{x:Bind Title}"
        BorderBrush="{x:Bind helpers:XamlHelpers.GetChoiceCardBorderBrush(IsSelected)}"
        BorderThickness="{x:Bind helpers:XamlHelpers.GetChoiceCardBorderThickness(IsSelected)}"
        Command="{x:Bind Command}"
        CommandParameter="{x:Bind CommandParameter}"
        CornerRadius="8">
        <StackPanel Spacing="8">
            <Grid ColumnSpacing="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <FontIcon
                    VerticalAlignment="Center"
                    FontFamily="{StaticResource SymbolThemeFontFamily}"
                    FontSize="18"
                    Glyph="{x:Bind Glyph}" />

                <TextBlock
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    FontWeight="SemiBold"
                    Text="{x:Bind Title}"
                    TextWrapping="WrapWholeWords" />

                <FontIcon
                    Grid.Column="2"
                    VerticalAlignment="Center"
                    FontFamily="{StaticResource SymbolThemeFontFamily}"
                    FontSize="16"
                    Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                    Glyph="&#xE73E;"
                    Visibility="{x:Bind IsSelected}" />
            </Grid>

            <TextBlock
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Style="{StaticResource CaptionTextBlockStyle}"
                Text="{x:Bind Description}"
                TextWrapping="WrapWholeWords" />

            <Border
                Padding="8,1"
                HorizontalAlignment="Left"
                Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                BorderBrush="{ThemeResource AccentControlElevationBorderBrush}"
                BorderThickness="1"
                CornerRadius="11"
                Visibility="{x:Bind IsRecommended}">
                <TextBlock
                    Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                    Style="{StaticResource CaptionTextBlockStyle}"
                    Text="{x:Bind domain:Translator.ProviderSelection_Choice_Recommended, Mode=OneTime}" />
            </Border>
        </StackPanel>
    </Button>
</UserControl>
