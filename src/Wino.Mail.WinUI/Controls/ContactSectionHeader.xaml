<UserControl
    x:Class="Wino.Controls.ContactSectionHeader"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="using:Wino.Helpers"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="root"
    HorizontalAlignment="Stretch"
    HorizontalContentAlignment="Stretch"
    x:DefaultBindMode="OneWay"
    mc:Ignorable="d">

    <Grid ColumnSpacing="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <!--  Icon tile: gives every section a scannable identity on the left edge.  -->
        <Border
            Width="32"
            Height="32"
            VerticalAlignment="Center"
            Background="{ThemeResource SubtleFillColorSecondaryBrush}"
            CornerRadius="6">
            <FontIcon
                FontFamily="{StaticResource SymbolThemeFontFamily}"
                FontSize="16"
                Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                Glyph="{x:Bind Glyph}" />
        </Border>

        <StackPanel Grid.Column="1" VerticalAlignment="Center">
            <TextBlock
                Style="{StaticResource BodyStrongTextBlockStyle}"
                Text="{x:Bind Title}"
                TextTrimming="CharacterEllipsis" />
            <TextBlock
                Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                Style="{StaticResource CaptionTextBlockStyle}"
                Text="{x:Bind Description}"
                TextTrimming="CharacterEllipsis" />
        </StackPanel>

        <!--  Entry count, so a collapsed section still says what is inside it.  -->
        <Border
            Grid.Column="2"
            MinWidth="20"
            Height="20"
            Padding="7,0"
            VerticalAlignment="Center"
            Background="{ThemeResource SubtleFillColorSecondaryBrush}"
            CornerRadius="10"
            Visibility="{x:Bind helpers:XamlHelpers.CountToVisibilityConverter(ItemCount)}">
            <TextBlock
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                FontWeight="SemiBold"
                Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                Style="{StaticResource CaptionTextBlockStyle}"
                Text="{x:Bind helpers:XamlHelpers.CountToText(ItemCount)}" />
        </Border>
    </Grid>
</UserControl>
