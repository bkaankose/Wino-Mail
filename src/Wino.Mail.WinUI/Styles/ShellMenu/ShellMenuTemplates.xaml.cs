namespace Wino.Mail.WinUI.Styles.ShellMenu;

/// <summary>
/// Code-behind exists only because x:Bind requires it inside a resource dictionary.
/// </summary>
public sealed partial class ShellMenuTemplates
{
    public ShellMenuTemplates()
    {
        InitializeComponent();
    }
}
