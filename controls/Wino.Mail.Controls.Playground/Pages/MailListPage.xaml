<Page
    x:Class="Wino.Mail.Controls.Playground.Pages.MailListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:automation="using:Microsoft.UI.Xaml.Automation"
    xmlns:controls="using:Wino.Mail.Controls.MailListView"
    xmlns:core="using:Wino.Mail.Controls.Core"
    xmlns:local="using:Wino.Mail.Controls.Playground.Pages">
    <Grid Padding="32" RowSpacing="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Mail list" />
        <TextBlock Grid.Row="1"
                   Style="{StaticResource BodyTextBlockStyle}"
                   Text="Select a row or expand a thread to inspect projection, threading, and selection behavior." />
        <controls:WinoMailListView Grid.Row="2"
                                   automation:AutomationProperties.Name="Mail list control demo"
                                   MailItemsSource="{x:Bind ViewModel.Items}"
                                   ProjectionOptions="{x:Bind ViewModel.ProjectionOptions}">
            <controls:WinoMailListView.SingleItemTemplate>
                <DataTemplate x:DataType="core:MailListRow">
                    <Grid Padding="12,8" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <FontIcon Glyph="&#xE8A5;" />
                        <TextBlock Grid.Column="1" Text="{x:Bind SourceItem.NameSortKey}" />
                    </Grid>
                </DataTemplate>
            </controls:WinoMailListView.SingleItemTemplate>
            <controls:WinoMailListView.ThreadHeaderTemplate>
                <DataTemplate x:DataType="core:MailListRow">
                    <Grid Padding="12,8" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <FontIcon Glyph="&#xE8B3;" />
                        <TextBlock Grid.Column="1" FontWeight="SemiBold" Text="{x:Bind SourceItem.NameSortKey}" />
                        <TextBlock Grid.Column="2" Text="{x:Bind Thread.Count}" />
                    </Grid>
                </DataTemplate>
            </controls:WinoMailListView.ThreadHeaderTemplate>
            <controls:WinoMailListView.ThreadChildTemplate>
                <DataTemplate x:DataType="core:MailListRow">
                    <TextBlock Margin="48,8,12,8" Opacity="0.72" Text="{x:Bind SourceItem.NameSortKey}" />
                </DataTemplate>
            </controls:WinoMailListView.ThreadChildTemplate>
        </controls:WinoMailListView>
    </Grid>
</Page>
