<abstract:SignatureAndEncryptionPageAbstract
    x:Class="Wino.Views.Settings.SignatureAndEncryptionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:abstract="using:Wino.Views.Abstract"
    xmlns:domain="using:Wino.Core.Domain"
    mc:Ignorable="d">

    <Grid Padding="24">
        <StackPanel Spacing="24">
            <controls:SettingsExpander Header="{x:Bind domain:Translator.SettingsSignatureAndEncryption_MyCertificatesHeader, Mode=OneWay}" Description="{x:Bind domain:Translator.SettingsSignatureAndEncryption_MyCertificatesDescription, Mode=OneWay}" IsExpanded="True">
                <controls:SettingsExpander.Items>
                    <StackPanel Spacing="12" Padding="12">
                        <!-- TODO: Not working -->
                        <!-- <TextBlock Visibility="{Binding ViewModel.PersonalCertificatesEmpty, Mode=OneWay}"> -->
                        <!--     <Run Text="There are no items." /> -->
                        <!-- </TextBlock> -->
                        <ListView x:Name="PersonalCertList"
                                      ItemsSource="{x:Bind ViewModel.PersonalCertificates, Mode=OneWay}" Height="180"
                                      SelectionMode="Multiple" SelectionChanged="PersonalCertList_SelectionChanged">
                            <ListView.Header>
                                <Grid Padding="16,12"
                                      ColumnSpacing="16">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="26" />
                                        <ColumnDefinition Width="300" />
                                        <ColumnDefinition Width="120" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="1"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                               Text="{x:Bind domain:Translator.SettingsSignatureAndEncryption_NameColumn, Mode=OneWay}" />
                                    <TextBlock Grid.Column="2"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                               Text="{x:Bind domain:Translator.SettingsSignatureAndEncryption_ExpiresColumn, Mode=OneWay}" />
                                    <TextBlock Grid.Column="3"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                               Text="{x:Bind domain:Translator.SettingsSignatureAndEncryption_ThumbprintColumn, Mode=OneWay}" />
                                </Grid>
                            </ListView.Header>
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Grid ColumnSpacing="16">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="0" />
                                            <ColumnDefinition Width="300" />
                                            <ColumnDefinition Width="120" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding Subject}" Grid.Column="1" />
                                        <TextBlock Text="{Binding NotAfter}" Grid.Column="2" FontSize="10" Foreground="Gray" />
                                        <TextBlock Text="{Binding Thumbprint}" Grid.Column="3" FontSize="10" Foreground="Gray" />
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>

                            <ListView.ItemContainerStyle>
                                <Style BasedOn="{StaticResource DefaultListViewItemStyle}"
                                       TargetType="ListViewItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                </Style>
                            </ListView.ItemContainerStyle>
                        </ListView>
                        <StackPanel Orientation="Horizontal" Spacing="12" HorizontalAlignment="Right">
                            <Button Click="RemovePersonalCertificates_Click">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <SymbolIcon Symbol="Delete" />
                                    <TextBlock Text="{x:Bind domain:Translator.Buttons_Remove, Mode=OneWay}" />
                                </StackPanel>
                            </Button>
                            <Button Click="ExportPersonalCertificates_Click">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <SymbolIcon Symbol="SaveLocal" />
                                    <TextBlock Text="{x:Bind domain:Translator.Buttons_Export, Mode=OneWay}" />
                                </StackPanel>
                            </Button>
                            <Button Click="ImportPersonalCertificates_Click">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <SymbolIcon Symbol="Import" />
                                    <TextBlock Text="{x:Bind domain:Translator.Buttons_Import, Mode=OneWay}" />
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </StackPanel>
                </controls:SettingsExpander.Items>
            </controls:SettingsExpander>
            <controls:SettingsExpander Header="{x:Bind domain:Translator.SettingsSignatureAndEncryption_RecipientCertificatesHeader, Mode=OneWay}" Description="{x:Bind domain:Translator.SettingsSignatureAndEncryption_RecipientCertificatesDescription, Mode=OneWay}" IsExpanded="False">
                <controls:SettingsExpander.Items>
                    <StackPanel Spacing="12" Padding="12">
                        <!-- TODO: Not working -->
                        <!-- <TextBlock Visibility="{Binding ViewModel.RecipientCertificatesEmpty, Mode=OneWay}"> -->
                        <!--     <Run Text="There are no items." /> -->
                        <!-- </TextBlock> -->
                        <ListView x:Name="RecipientCertList"
                      ItemsSource="{x:Bind ViewModel.RecipientCertificates, Mode=OneWay}" Height="180"
                      SelectionMode="Multiple" SelectionChanged="RecipientCertList_SelectionChanged">
                            <ListView.Header>
                                <Grid Padding="16,12"
                                      ColumnSpacing="16">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="26" />
                                        <ColumnDefinition Width="300" />
                                        <ColumnDefinition Width="120" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="1"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                               Text="{x:Bind domain:Translator.SettingsSignatureAndEncryption_NameColumn, Mode=OneWay}" />
                                    <TextBlock Grid.Column="2"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                               Text="{x:Bind domain:Translator.SettingsSignatureAndEncryption_ExpiresColumn, Mode=OneWay}" />
                                    <TextBlock Grid.Column="3"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                               Text="{x:Bind domain:Translator.SettingsSignatureAndEncryption_ThumbprintColumn, Mode=OneWay}" />
                                </Grid>
                            </ListView.Header>
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Grid ColumnSpacing="16">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="0" />
                                            <ColumnDefinition Width="300" />
                                            <ColumnDefinition Width="120" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding Subject}" Grid.Column="1" />
                                        <TextBlock Text="{Binding NotAfter}" Grid.Column="2" FontSize="10" Foreground="Gray" />
                                        <TextBlock Text="{Binding Thumbprint}" Grid.Column="3" FontSize="10" Foreground="Gray" />
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>

                            <ListView.ItemContainerStyle>
                                <Style BasedOn="{StaticResource DefaultListViewItemStyle}"
                                       TargetType="ListViewItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                </Style>
                            </ListView.ItemContainerStyle>
                        </ListView>
                        <StackPanel Orientation="Horizontal" Spacing="12" HorizontalAlignment="Right">
                            <Button Click="RemoveRecipientCertificates_Click">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <SymbolIcon Symbol="Delete" />
                                    <TextBlock Text="{x:Bind domain:Translator.Buttons_Remove, Mode=OneWay}" />
                                </StackPanel>
                            </Button>
                            <Button Click="ExportRecipientCertificates_Click">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <SymbolIcon Symbol="SaveLocal" />
                                    <TextBlock Text="{x:Bind domain:Translator.Buttons_Export, Mode=OneWay}" />
                                </StackPanel>
                            </Button>
                            <Button Click="ImportRecipientCertificates_Click">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <SymbolIcon Symbol="Import" />
                                    <TextBlock Text="{x:Bind domain:Translator.Buttons_Import, Mode=OneWay}" />
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </StackPanel>
                </controls:SettingsExpander.Items>
            </controls:SettingsExpander>
        </StackPanel>
    </Grid>
</abstract:SignatureAndEncryptionPageAbstract>
